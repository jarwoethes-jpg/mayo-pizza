{
  "name": "mayo.pizza",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "biome check . && pnpm --filter shared exec tsc --noEmit && pnpm --filter server exec tsc --noEmit && pnpm --filter web exec tsc --noEmit",
    "test": "pnpm --filter shared test && pnpm --filter server test",
    "test:e2e": "playwright test -c e2e/playwright.config.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@playwright/test": "^1.54.1",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22 <23"
  },
  "packageManager": "pnpm@11.2.2"
}

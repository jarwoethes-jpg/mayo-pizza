{
  "name": "mayo.pizza",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "pnpm --filter shared build && biome check . && pnpm --filter shared exec tsc --noEmit && pnpm --filter server exec tsc --noEmit && pnpm --filter web exec tsc --noEmit",
    "test": "pnpm --filter shared build && pnpm --filter shared test && pnpm --filter server test && pnpm --filter web test",
    "generate:headers": "node infra/generate-headers.mjs",
    "e2e:fixtures": "node scripts/e2e-fixtures.mjs",
    "e2e": "playwright test -c playwright.config.ts",
    "test:e2e": "pnpm e2e"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@biomejs/biome": "^2.1.3",
    "@playwright/test": "^1.61.1",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22 <23"
  },
  "packageManager": "pnpm@11.2.2"
}
